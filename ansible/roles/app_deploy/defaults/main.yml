---
# npm 镜像源（可在 group_vars/all.yml 中覆盖）
# 国内服务器默认使用 npmmirror 加速，海外可改为 https://registry.npmjs.org/
npm_registry: "https://registry.npmmirror.com"

# npm HTTP 代理（可选，留空表示不使用代理）
# 例如指向控制机上的局域网代理："http://192.168.10.111:7890"
npm_proxy: ""
